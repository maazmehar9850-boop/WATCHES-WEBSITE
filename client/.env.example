# Client — copy to `client/.env` for local overrides.
# NEVER commit secrets. Only VITE_* vars are exposed to the browser.

# Local dev: leave empty (Vite proxies /api → http://127.0.0.1:5000)
VITE_API_URL=

# Production (also set on Vercel → Project → Environment Variables):
# VITE_API_URL=https://watches-website-q2w3.vercel.app/api
