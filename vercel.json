{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": null,
  "installCommand": "rm -rf node_modules client/node_modules server/node_modules && npm install --legacy-peer-deps --no-audit --no-fund",
  "buildCommand": "npm run build --workspace=client && rm -rf dist && mkdir -p dist && cp -R client/dist/. dist/ && test -f dist/index.html && ls -la dist",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api" },
    { "source": "/uploads/(.*)", "destination": "/api" },
    { "source": "/(.*)", "destination": "/index.html" }
  ],
  "functions": {
    "api/index.js": {
      "includeFiles": "server/**",
      "maxDuration": 30
    }
  }
}
